package com.ppm.mes.domain.rt.rt300;

import javax.inject.Inject;

import org.springframework.stereotype.Service;

import com.ppm.mes.domain.BaseService;
 
@Service
public class RoutEquipService extends BaseService <RoutEquip,RoutEquip.RoutEquipId>{
	
    private RoutEquipRepository repository;
    
    @Inject
    public RoutEquipService(RoutEquipRepository repository) {
        super(repository);
        this.repository = repository;
    }
}